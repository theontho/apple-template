# Gemini Instructions

Follow `AGENTS.md`. This repository is a Swift template for iOS apps, macOS apps, and Swift CLIs.

Keep reusable code in `AppleTemplateCore`, CLI code in `AppleTemplateCLI`, and SwiftUI app code under `Apps/`.
